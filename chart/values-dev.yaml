replicaCount: 1

image:
  pullPolicy: Always

ingress:
  enabled: true
  host: insider-service.dev.local

resources:
  requests:
    cpu: 50m
    memory: 32Mi
  limits:
    cpu: 200m
    memory: 64Mi
