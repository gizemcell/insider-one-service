apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "pingsvc.name" . }}
  labels:
    {{- include "pingsvc.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "pingsvc.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "pingsvc.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: pingsvc
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.targetPort }}
              protocol: TCP
          envFrom:
            - configMapRef:
                name: {{ include "pingsvc.name" . }}
            {{- if .Values.secret }}
            - secretRef:
                name: {{ include "pingsvc.name" . }}
            {{- end }}
          livenessProbe:
            httpGet:
              path: /ping
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources: {}
